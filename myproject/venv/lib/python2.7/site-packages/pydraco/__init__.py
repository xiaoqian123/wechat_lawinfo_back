
from core import Connection
from error import (Error, Warning, InterfaceError, DataError)

paramstyle = 'qmark'

apilevel = '2.0'

threadsafety = 3

def connect(user='crate', host="localhost", port=5432, database=None, password=None,ssl=False, 
            timeout=None, application_name=None):
    return Connection(user, host, port, database, password, ssl, timeout, application_name)
